The folder OX contains results for the EGL and EGL-L instances by applying the OX operator

The folder TGTBCX contains results for the EGL and EGL-L instances by applying the TGTBCX operator

The .txt file [NEW BKS CARP] contains the new obtained best known solutions for the CARP